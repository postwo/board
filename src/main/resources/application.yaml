  debug: false
management.endpoints.web.exposure.include: "*"
logging:
  level:
    org.example.boardproject: debug
    org.springframework.web.servlet: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/board
    username: boards
    password: "@cce7717"
    driver-class-name: com.mysql.cj.jdbc.Driver

  #  datasource:    #h2 testdb???? ?
  #      url: jdbc:h2:mem:testdb
  #      username: sa
  #      driver-class-name: org.h2.Driver

  jpa:
    defer-datasource-initialization: true
    hibernate.ddl-auto: create
    #open-in-view: false
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.default_batch_fetch_size: 100
  h2.console.enabled: false #???? false h2 db ?? ??? true? ??
  sql.init.mode: always
  data.rest: #dataRepository?  dependencies? ?? ?? ?? ?? ???
    base-path: /api #??? http://localhost:8080/api ?????? ?????? /api? ???? ???
    detection-strategy: annotated  #annotated? ????? ??? ?? ?? ???
  thymeleaf3:
    decoupled-logic: true
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_OAUTH_CLIENT_ID}
            client-secret: ${KAKAO_OAUTH_CLIENT_SECRET}
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            client-authentication-method: POST
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id







  #data.rest:
    # base-path: /api
    #detection-strategy: annotated

---

#?? db ???? ?? ????
#spring:
#  config.activate.on-profile: testdb
#  datasource:
#    url: jdbc:h2:mem:board;mode=mysql
#    driverClassName: org.h2.Driver
#  sql.init.mode: always
#  test.database.replace: none